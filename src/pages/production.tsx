import { FC } from "react";
import { Space, Typography } from "antd";

const ProductionPage: FC = () => {
  return (
    <Space>
      <Typography>Hello, production!</Typography>
    </Space>
  );
};

export default ProductionPage;
